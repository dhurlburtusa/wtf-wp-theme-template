<?php
if ( ! defined( 'ABSPATH' ) ) { http_response_code(404); die(); }

/*
TODO: Fill in whatever code belongs at the start of the document body. You may
want to include a call to `wp_body_open`.

Note: It is not recommended to use the pattern where an HTML tag is opened in
this template and then later closed in another template, such as the
`document_body_end-{wtf__document_body_start_tpl_name}.php` template.
*/

// wp_body_open();
?>
<noscript>
	<p><?php _e( 'In order for this site to function properly, JavaScript must be enabled.', 'wtf' ); ?></p>
</noscript>
